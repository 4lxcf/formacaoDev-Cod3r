import router from "../../services/fetch";
export async function getHref(href) {
    try {
        const { data } = await router.get(href);
        return { html: data };
    }
    catch (error) {
        return { err: error.message };
    }
}
