"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.topicsScrap = void 0;
var cheerio_1 = require("cheerio");
function topicsScrap(html) {
    var $ = (0, cheerio_1.load)(html);
    var categoryP = "";
    var result = [];
    $("#content > ul > li")
        .each(function (i, e) {
        var category = $(e).find(".title-bg").text().trim();
        categoryP = category != "" ? category : categoryP;
        if (category != "")
            return;
        result.push({
            href: $(e).find(".list-item > a").first().attr("href") || "",
            name: $(e).find(".list-item > a").first().text().trim(),
            category: categoryP,
        });
    })
        .toArray();
    return result;
}
exports.topicsScrap = topicsScrap;
