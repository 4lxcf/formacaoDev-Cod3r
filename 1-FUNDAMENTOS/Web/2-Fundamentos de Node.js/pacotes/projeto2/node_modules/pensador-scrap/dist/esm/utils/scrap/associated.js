import { formatUrl } from "./format-url";
export const scrapAssociated = ($) => {
    return $("div.row > .sidebar > .list-boxed > .list-item > a")
        .map((i, e) => formatUrl($(e).attr("href")))
        .toArray();
};
