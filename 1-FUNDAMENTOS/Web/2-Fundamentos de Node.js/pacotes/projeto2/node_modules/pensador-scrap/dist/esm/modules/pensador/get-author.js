import router from "../../services/fetch";
import { checkString } from "../../utils";
export async function getAuthor(author) {
    try {
        if (!author || checkString(author)) {
            throw new Error("essa query não é válido.");
        }
        const { data } = await router.get("/autor/" + author.toLowerCase().replace(/[ ]/gi, "_"));
        return { html: data };
    }
    catch (error) {
        return { err: error.message };
    }
}
